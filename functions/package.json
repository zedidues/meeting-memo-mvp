{
  "name": "meeting-memo-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-functions": "^6.0.1",
    "firebase-admin": "^12.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "firebase-tools": "^14.5.1",
    "typescript": "^5.6.3"
  }
}
